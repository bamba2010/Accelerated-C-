#include <iostream>
// a program when run will writes "This (") is a quote , and this (\) is a backslash
int main() {
    std::cout << "This ( \") is a quote and this (\\) is a backslash"<< std::endl;
    0;
}