#include <iostream>
// This program woks perfectly because as long as the number of curly braces from left and right are equal.
int main() {{{{{{
                        std::cout << "Hello, World!" << std::endl;
                        return 0;
                    }}}}}}